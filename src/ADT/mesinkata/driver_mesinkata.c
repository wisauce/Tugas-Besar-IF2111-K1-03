#include "mesinkata.h"
#include <stdio.h>

int main()
{
    /** test baca dari file */
    // STARTWORDFILE("save1.txt");
    // while (!EndWord)
    // {
    //     printw(currentWord, true);
    //     ADVWORDFILE();
    // }

    /* test baca dari input */

    // STARTWORD();
    // while (!EndWord)
    // {
    //     printw(currentWord, true);
    //     ADVWORD();
    // }

    STARTLINE();
    printw(currentWord, true);
}